name: USD v26.03

on:
  push:
    paths-ignore:
      - '**.md'
  pull_request:
    paths-ignore:
      - '**.md'

jobs:
  run-tests:
    name: Run Tests for USD v26.03
    uses: ./.github/workflows/run-tests.yml
    with:
      usd-version: 26.03
      test-filter: '-Draco|_Below2502|_Equal2502'
